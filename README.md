# To build Image Recognition Systems using OpenCV libraries

## Step 1: Prepare a smiling face image dataset. 

The dataset should has 3 images, and each image should feature one or more smiling faces. We can search and copy image URLs using Unsplash.

## Step 2: Set up an OpenCV Haar classifier using an appropriate pre-trained classifier for detecting smiles (see GitHub > OpenCV > Data > Haar cascades).

## Step 3: Perform smile detection for each image and display the detections as boxes drawn on the image. Experiment with the detectMultiScale() function parameters to improve your detections.
